/** @type {import('next').NextConfig} */
const nextConfig = {}

// module.exports = nextConfig
module.exports = {
    distDir: 'build',
    env: {
      basePath: 'https://api.7travelmoney.com',
    },
  }
